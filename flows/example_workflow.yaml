flow: ExampleWorkflow
description: Test flow to verify PascalCase naming

inputs:
  - name: input_value
    type: string
    required: true

steps:
  - task: ProcessInput
    id: process
    inputs:
      value: ${inputs.input_value}
    outputs:
      - result

outputs:
  - name: output
    value: ${process.result}
